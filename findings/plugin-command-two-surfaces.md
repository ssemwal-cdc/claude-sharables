---
id: F64
slug: plugin-command-two-surfaces
kind: finding
status: observed
date: 2026-08-24
---
# Two plugin commands exist

**Outcome protected.** An instruction names the surface it works on.

**Argument.**

The slash command is typed in a chat. The CLI command is typed in a terminal. They are different things.

A sheet that says terminal and then shows the slash command is wrong.

`README.md` used to make exactly that mistake.

**Evidence.**

Reported by the maintainer, date unrecorded.
The maintainer has typed the wrong command on the wrong surface themself.
The run is unmeasured.

**Checks.** none
